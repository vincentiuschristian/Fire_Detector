<resources>
    <!-- Base application theme. -->
    <style name="Theme.FireDetector" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onprimary</item>
        <item name="colorSurface">@color/container</item>
        <item name="colorPrimaryContainer">@color/primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/onprimaryContainer</item>

        <item name="colorOutlineVariant">@color/outline</item>

        <!-- Status bar color. -->
        <!--        <item name="android:statusBarColor">@color/white</item>-->
        <!-- Customize your theme here. -->
    </style>

    <style name="MaterialAlertDialog_rounded" parent="@style/ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="shapeAppearanceOverlay">@style/DialogCorners</item>
    </style>

    <style name="DialogCorners">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>



    <style name="CustomEditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/bg_edit_text</item>
        <item name="android:textColorHint">#757575</item>
        <item name="android:textSize">16sp</item>
        <item name="textInputStyle">@style/Widget.Material3.TextInputEditText.OutlinedBox</item>
        <item name="android:padding">20dp</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="BottomNavigationStyle">

        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">62dp</item>
        <item name="itemActiveIndicatorStyle">?attr/background</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:elevation">36dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="labelVisibilityMode">labeled</item>
        <item name="menu">@menu/bottom_nav_menu</item>
        <item name="bottomNavigationStyle">@style/Widget.Material3.BottomNavigationView</item>

    </style>


    <!-- Base application theme. -->
    <style name="Base.Theme.FireDetector" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>